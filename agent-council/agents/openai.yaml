interface:
  display_name: "Agent Council"
  short_description: "Run Claude Code and OpenCode reviews from one skill"
  default_prompt: "$agent-council council "
policy:
  allow_implicit_invocation: false
